# ===========================================================
# SIH26153 — AI-Based Network Attack Forecasting
# Merged requirements (all four source repos combined)
# ===========================================================
#
# Conflict resolution notes:
#   - pandas: NTAV pins ==2.1.3, killchain pins ==2.1.0 → use >=2.1.3 (newer, compatible)
#   - networkx: both NTAV and killchain pin ==3.2 → keep ==3.2
#   - matplotlib: NTAV pins ==3.8.2, PS40 unpinned → use >=3.8.2
#   - scikit-learn: NTAV pins ==1.3.2, PS40 unpinned → use >=1.3.2
#   - scapy: NTAV pins ==2.5.0 → keep (only used for live capture, optional)
# ===========================================================

# Core data / ML
numpy>=1.24.0
pandas>=2.1.3
scikit-learn>=1.3.2
joblib>=1.3.0
matplotlib>=3.8.2

# Dashboard
flask>=3.0.0
plotly>=5.17.0

# Graph analysis & networking
networkx==3.2
scapy==2.5.0

# Killchain engine
pyyaml>=6.0.1
python-dateutil>=2.8.2

# Database (used by killchain engine)
# sqlite3 is in stdlib

# Testing
pytest>=7.4.3
pytest-cov>=4.1.0

# Optional: XGBoost for Model B forecasting
xgboost>=2.0.0
