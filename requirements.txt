# SIH26153 — AI-Based Network Attack Forecasting
# Top-level requirements (pin to tested versions)
#
# Conflict resolution notes:
#   pandas:       NTAV==2.1.3 vs killchain==2.1.0  → 2.1.4 (latest 2.1.x, backward compatible)
#   scikit-learn: NTAV==1.3.2, PS40 unpinned       → 1.4.2 (latest 1.x stable)
#   matplotlib:   NTAV==3.8.2, PS40 unpinned       → 3.8.4
#   networkx:     NTAV==3.2, killchain==3.2         → 3.2 (exact, both repos agree)
#   scapy:        NTAV==2.5.0                       → 2.5.0 (live-capture only, optional)

# ── Web framework ──────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1

# ── ML / Data ──────────────────────────────────────────────
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.4.2
joblib==1.4.2
matplotlib==3.8.4

# ── Forecasting extras ─────────────────────────────────────
xgboost==2.0.3

# ── Visualization ──────────────────────────────────────────
plotly==5.22.0

# ── Graph ──────────────────────────────────────────────────
networkx==3.2

# ── Killchain engine deps ──────────────────────────────────
pyyaml==6.0.1
python-dateutil==2.9.0

# ── Packet capture (needs admin/root for live capture) ─────
scapy==2.5.0

# ── Testing ────────────────────────────────────────────────
pytest==7.4.4
pytest-cov==4.1.0

# ── Production server ──────────────────────────────────────
gunicorn==22.0.0
